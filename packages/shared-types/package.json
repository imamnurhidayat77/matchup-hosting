{
  "name": "@matchup/shared-types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types, enums, and DTO placeholders used across MatchUp apps.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests yet'"
  }
}