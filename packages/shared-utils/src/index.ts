/**
 * Small reusable helpers shared across apps.
 *
 * Keep additions narrow — anything added here should be obviously reusable
 * (e.g. date formatting, string casing) rather than domain-specific.
 */

export * from './string';
export * from './date';