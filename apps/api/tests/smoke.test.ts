/**
 * Placeholder smoke test. Real tests (supertest, jest/vitest setup) will be
 * added during MVP implementation.
 *
 * This file exists so CI can run a basic `npm test` without errors and so
 * the boilerplate has a clear slot for the test harness.
 */
export {};